<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="style.css">
    <title>{{ headingTitle }}</title>
  </head>
  <body>
    {{#if home}}
      {{> hero}}
    {{/if}}
    {{> header}}
    {{> (lookup . 'content')}}
    {{> footer}}
  </body>
</html>
